title: action-logout
type: fragment
steps:
  - include: goto-account-page
  - click:
      selector: ${accountPageSelectorControllLogout}
      text: ${accountPageControlTextLogout}
  - include: ensure-at-login-page
